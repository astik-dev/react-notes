import classes from "./Logo.module.scss";

const Logo = () => {
    
    return (
        <div className={classes.logo}>
            <img src="/react.svg" alt="React icon" />
            Notes
        </div>
    )
};

export default Logo;
